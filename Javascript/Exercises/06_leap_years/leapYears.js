// let leapYears = (year)=>{
//     if(year % 4 == 0 && (year % 400) % 4 == 0){
//         // return `${year} is a leap year`
//         return true;
//     }else{
//         return false;
//     }
// };

// leapYears(2000)

// module.exports = leapYears;

let leapYears = (year)=>{
    if((year / 100)% 4 == 0){
        console.log('leap year')
        // this is the case for 2100 2000 and so on
    }else if(){
    }
}

leapYears(1900)